#servidor
server:
  port: 8082
  servlet:
    context-path: /comentario

spring:

  #banco de dados
  datasource:
    url: jdbc:h2:mem:post

  jpa:
    database: H2
    show-sql: true
    properties:
      format_sql: true
  flyway:
    enabled: true

